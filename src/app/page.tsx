import Wrap from "@/components/Wrap/Wrap";

/**
 * Renders the Home component, which displays the main content of the application.
 * and i use the wrap component to let Home Component be seo friendly
 * @return {JSX.Element} The JSX element representing the Home component.
 */

export default function Home() {
  return (
    <main>
      <Wrap />
    </main>
  );
}
